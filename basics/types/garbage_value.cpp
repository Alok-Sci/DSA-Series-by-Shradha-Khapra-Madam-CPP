#include <iostream>
using namespace std;

// * Garbage value: the value that is stored in the memory before the variable is assigned any value (i.e. random value - not 0)


int main(){
    int name;
    cout << name << endl;

    return 0;
}